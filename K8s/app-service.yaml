apiVersion: v1
kind: Service
metadata:
  name: recruitment-app-service
spec:
  selector:
    app: recruitment-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
  type: LoadBalancer
